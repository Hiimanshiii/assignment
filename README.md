Cal.com clone monorepo structure.

This project contains:
- `frontend` – React + Vite + TailwindCSS app
- `backend` – Node.js + Express.js + MySQL API

Code is not implemented yet; this repository currently defines only the folder and file structure.
